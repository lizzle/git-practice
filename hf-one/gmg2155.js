var student = "Grace";
var status = "first year graduate";
var major = "Instructional Technology";
saveMyProfile (student, status, major);

var age = 24;
function addOne(x)
{
  x=x+2;
}

var status = bake(400);

function bake (degrees)
{
  var message;

if (degress > 450)
{
  message = "It's burning!";
}
else if (degrees <100)
{
  message="Turn up the heat!";
}
else {
  message = "Perfecto!";
}
 return message;
}
